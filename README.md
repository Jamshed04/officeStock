В репозиторий не удалось добавить папку с ML-данными (`ml_data`) из-за её размера и ограничений репозитория.  

Чтобы проект работал корректно, нужно сделать следующее:

1. Скачать/скопировать папку `ml_data` из внешнего источника.
2. Поместите её в директорию `ml` внутри проекта